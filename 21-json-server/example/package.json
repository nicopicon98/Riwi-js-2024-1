{
  "name": "example",
  "version": "1.0.0",
  "description": "Bueno chicos, vamos a entender a JSON-SERVER como esta herramienta que nos va a facilitar el desarrollo, reduciendo el cuellode botella que se genera cuando el front-end y el back-end no están sincronizados.",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch database.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
