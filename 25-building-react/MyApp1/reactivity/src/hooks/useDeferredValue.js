let currentComponent = null;

// Función hook useDeferredValue
export function useDeferredValue(value) {
  // Retornar el valor tal cual
  return value;
}

// Vamos a explicar el código anterior:

// 1. Creamos una variable global `currentComponent` para almacenar la referencia al componente actual.
// 2. La función `useDeferredValue` recibe un `value` y simplemente retorna ese valor.
// 3. Esta es una implementación simplificada que no maneja realmente la diferición de valores.
